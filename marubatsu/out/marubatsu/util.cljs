(ns ;;marubatsu.cljs.util
    marubatsu.util
    ;;(:require-macros [marubatsu.clj.util :as util])
    (:require-macros [marubatsu.util]))

(defn hw3 []
  (do
    ;;(u/hw2)
    (println "Hello world2!")))
